extend type Query {
  set(id: ID!) : Set
}

type Set {
  code: ID!
  name: String
  ptcgoCode: String
  series: String
  totalCards: Int
  standardLegal: Boolean
  expandedLegal: Boolean
  updatedSince: String
  releaseDate: String
  symbolUrl: String
  logoUrl: String
  updatedAt: String
}